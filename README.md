# downloadd
Download Daemon is a Linux file system monitoring daemon written in modern C++ that uses the inotify API to recursively watch directories and log file system events such as file creation, deletion, modification, moves, and renames with timestamps.
